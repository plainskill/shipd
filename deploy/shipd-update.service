[Unit]
Description=shipd self-update from the local registry
After=network-online.target docker.service
Wants=network-online.target

[Service]
Type=oneshot
ExecStart=/stack/compose/shipd/update.sh
# a failed update must be visible, not silent
StandardOutput=journal
StandardError=journal
